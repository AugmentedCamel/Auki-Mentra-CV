export function toSimplified(text: string): string {
  // TODO: convert to simplified Chinese
  return text;
}

export function toTraditional(text: string): string {
  // TODO: convert to traditional Chinese
  return text;
}
